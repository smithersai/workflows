# Re-request Stack PR Reviews

Fixes were pushed across the stack. Post a single re-review comment on **each PR
that changed** so reviewers re-evaluate with full context.

Addressed this round (JSON, has `pr` per item):
{props.addressed}

Intentionally skipped (JSON, has `pr` per item):
{props.skipped}

For each distinct PR number that appears in `addressed` (and any PR whose branch
was pushed), post one comment with `gh pr comment <pr>`. Each comment MUST:

1. Start **exactly** with the re-review trigger mention (interpolated handles, do
   not alter):
   {props.reviewersMention} re-review
2. Summarize what was addressed **on that PR** — one bullet per item (only the
   findings whose `pr` matches).
3. Explicitly call out anything intentionally skipped on that PR and why, so
   reviewers can re-review with that context.

Keep each comment concise and factual. Do NOT merge any PR — never run
`gh pr merge` or enable auto-merge. Posting the comments is the only action.

Report each PR you commented on (with the comment URL) in `rerequested`.

REQUIRED OUTPUT:
{props.schema}

Return ONLY raw JSON matching the required output schema.
