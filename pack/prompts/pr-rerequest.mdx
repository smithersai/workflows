# Re-request PR Review

Fixes have been pushed to pull request **#{props.prNumber}**. Post a single
re-review comment so the reviewers re-evaluate with full context.

Post with `gh pr comment {props.prNumber} --body "..."`. The comment MUST:

1. Start with the re-review trigger mention: `{props.reviewersMention} re-review`.
2. Summarize **what was addressed** (one bullet per item):
{props.addressed}
3. Explicitly call out **what was intentionally skipped and why**, so reviewers
   can re-review with that in mind:
{props.skipped}

Keep it concise and factual. Do NOT merge the PR — never run `gh pr merge` or
enable auto-merge. Posting the comment is the only action.

REQUIRED OUTPUT:
{props.schema}

Return ONLY raw JSON matching the required output schema.
