# Implement

Carry out the following request in the current repository.

ANONYMITY: Never name the tooling behind this work in anything others will see — commit messages,
PR titles or bodies, code comments, or branch names. No `xiv`, no `smithers`, no internal workflow
or command names. It's a private internal tool; the work must read as ordinary human-authored code.

REQUEST:
{props.prompt}

REQUIRED OUTPUT:
{props.schema}
