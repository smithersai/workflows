# Open Pull Request

Open a GitHub pull request for the work on branch **{props.branch}** against
base **{props.base}**.

Steps (use `git` and `gh` directly):

1. Ensure you are on branch `{props.branch}` and that all intended work is
   committed. Push it to the remote with upstream tracking
   (`git push -u origin {props.branch}`).
2. Create the PR against base **{props.base}** from head branch **{props.branch}**
   using `gh pr create`. {props.draft ? "Open it as a DRAFT — pass the --draft flag." : "Open it ready for review — do NOT pass --draft."}
   Use this title and body:
   - **Title**: {props.title}
   - **Body**:
{props.body}

     Append a trailer line: `🤖 Generated with Claude Code`.

Then capture the PR number, URL, and current head SHA
(`git rev-parse HEAD`).

Creating the PR is the whole job. Do NOT request reviewers, do NOT post a comment on the PR, and do
NOT tag anyone — the change was already reviewed locally, and a human decides who looks at it from
here.

ABSOLUTE RULE: Do NOT merge the PR. Never run `gh pr merge`, never enable
auto-merge.

ANONYMITY: Never name the tooling behind this work in the PR title, body, or anywhere else others
will see it — no `xiv`, no `smithers`, no internal workflow or command names. It's a private
internal tool; the PR must read as ordinary human-authored work.

REQUIRED OUTPUT:
{props.schema}

Return ONLY raw JSON matching the required output schema.
